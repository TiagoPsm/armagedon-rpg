server/node_modules/
node_modules/
test-results/
server/.env

# Credenciais do smoke de producao (tools/run-online-tests.ps1).
# O modelo versionado e o .env.example, sem senha nenhuma.
.env
server/*.log
*.log
_site/

# Wrangler local dev
cloudflare/.dev.vars
cloudflare/.wrangler/
.wrangler/

# Videos locais (assets pesados fora do repo)
*.mp4

# Snapshot do Obsidian: gerado pelo hook a cada commit e sempre com o hash do
# commit que acabou de nascer — nunca cabe dentro do proprio commit, entao
# vivia eternamente "modificado". E contexto local, nao fonte.
docs/obsidian/10-SNAPSHOT-AUTOMATICO.md
